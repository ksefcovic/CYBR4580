# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On macOS with MacPorts:
#   gem install pg -- --with-pg-config=/opt/local/lib/postgresql84/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem 'pg'
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: ddnmg2qdnfqd9h
  username: sfpmhkpypdyllp
  password: be1735b6e8e9d5cf1aff0ee57374d94e999f90c48a978c17f878ffd856121071
  host: ec2-174-129-242-183.compute-1.amazonaws.com
  port: 5432

test:
  <<: *default
  database: find_my_device_app_test

production:
  <<: *default
  database: find_my_device_app_production
  username: find_my_device_app
  password: <%= ENV['FIND_MY_DEVICE_APP_DATABASE_PASSWORD'] %>
