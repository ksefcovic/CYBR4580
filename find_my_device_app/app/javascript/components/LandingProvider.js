import React from "react"
import PropTypes from "prop-types"
class LandingProvider extends React.Component {
  render () {
    return (
      <React.Fragment>
        <h1>This is where landing page content will originate from => Place components in javascript/landingPage folder</h1>
        <h2>Sign in</h2>
        <h2>Create Account</h2>
      </React.Fragment>
    );
  }
}

export default LandingProvider
